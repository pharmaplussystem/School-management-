# School Management System - Corrected ZIP

Run `supabase/schema.sql` in Supabase SQL Editor. This version fixes the PostgreSQL `CREATE TYPE IF NOT EXISTS` error.

Then add your Supabase URL and anon/publishable key to `assets/js/config.js`.
